---
/**
 * Button — Multi-variant pill button (Apple-inspired).
 *
 * @prop variant - "primary" | "secondary" | "ghost"
 * @prop size - "sm" | "md" | "lg"
 * @prop href - If provided, renders as <a> instead of <button>
 * @prop class - Additional CSS classes
 * @prop type - Button type (only when not href)
 * @prop id - Unique ID for testing
 */
interface Props {
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  href?: string;
  class?: string;
  type?: 'button' | 'submit' | 'reset';
  id?: string;
}

const {
  variant = 'primary',
  size = 'md',
  href,
  class: className = '',
  type = 'button',
  id,
} = Astro.props;

const baseClasses = [
  'inline-flex items-center justify-center gap-2',
  'font-body font-semibold',
  'no-underline',
  'transition-all duration-200 ease-default',
  'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-500',
  'disabled:opacity-50 disabled:cursor-not-allowed',
  'cursor-pointer',
];

const variantClasses: Record<string, string> = {
  primary: [
    'bg-brand-500',
    'hover:bg-brand-600 hover:shadow-md',
    'active:scale-[0.98]',
  ].join(' '),
  secondary: [
    'border border-border-default bg-transparent',
    'text-text-primary',
    'hover:bg-surface-tertiary hover:border-border-strong',
    'active:scale-[0.98]',
  ].join(' '),
  ghost: [
    'bg-transparent text-text-link',
    'hover:text-text-link-hover',
  ].join(' '),
};

const sizeClasses: Record<string, string> = {
  sm: 'text-sm px-4 py-1.5 rounded-full',
  md: 'text-sm px-6 py-2.5 rounded-full',
  lg: 'text-base px-7 py-3 rounded-full',
};

const Tag = href ? 'a' : 'button';

/* Force white text on primary variant via inline style.
   This is necessary because the global a { color: ... } in @layer base
   can cascade into <a> tags. Even though @layer utilities should override,
   inline style is the safest guarantee for this critical contrast pair. */
const inlineStyle = variant === 'primary' ? 'color: #ffffff;' : undefined;
---

<Tag
  id={id}
  href={href}
  type={!href ? type : undefined}
  style={inlineStyle}
  class:list={[
    ...baseClasses,
    variantClasses[variant],
    sizeClasses[size],
    className,
  ]}
>
  <slot />
</Tag>
